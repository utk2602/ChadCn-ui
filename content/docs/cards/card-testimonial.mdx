---
title: Testimonial Card
description: Testimonial card with rating, block quote, and author's informations.
---

<ComponentPreview name="card-testimonial-demo" />

### Installation

<ComponentInstallation name="card-testimonial" />

## Usage

```tsx
<CardTestimonial
  testimonialQuote={TESTIMONIAL.quote}
  testimonialRating={TESTIMONIAL.rating}
  testimonialAuthor={{
    authorName: TESTIMONIAL.name,
    avatarUrl: TESTIMONIAL.avatarUrl,
    description: TESTIMONIAL.profession,
  }}
  role="article"
  aria-labelledby={`card-${TESTIMONIAL.id}-title`}
  aria-describedby={`card-${TESTIMONIAL.id}-content`}
  className="h-[450px] w-[350px] shadow"
>
  <TestimonialRating className="text-primary" />
  <div className="relative text-center mx-auto w-4/5 text-lg">
    <TestimonialQuote>{TESTIMONIAL.quote}</TestimonialQuote>
  </div>

  <TestimonialAuthor className="flex items-center gap-4" />
</CardTestimonial>
```

## Examples

<ComponentPreview name="card-testimonial-2-demo" />

## Props

#### CardTestimonial

<TypeTable
  type={{
    testimonialRating: {
      description: 'rating/note of the testimonial.',
      type: 'number',
      required: false,
    },
    testimonialQuote: {
      description: 'quote of the testimonial.',
      type: 'string',
      required: true,
    },
    testimonialAuthor: {
      description: 'author of the testimonial.',
      type: '{authorName: string;avatarUrl?: string;description?: string;}',
      required: true,
    },
  }}
/>
